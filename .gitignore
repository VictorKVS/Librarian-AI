# .gitignore

# 🛡️ Переменные окружения (НИКОГДА не коммитить)
.env                       # Основной файл окружения
.env.*                    # Все прочие .env-файлы (например, .env.dev)
!deploy/.env.template     # Шаблон .env, исключение из игнорирования

# 📦 Виртуальные окружения
venv/                     # Виртуальное окружение (стандартное имя)
.venv/                    # Виртуальное окружение (альтернативное имя)

# 🧪 Кеши, компиляция и временные файлы Python
__pycache__/              # Кеш интерпретатора Python
*.py[cod]                 # Скомпилированные Python-файлы (.pyc, .pyo, .pyd)
*.egg                     # Сборки пакетов
*.egg-info/              # Метаданные пакетов
*.log                     # Лог-файлы
*.sqlite3                # Временные SQLite-базы
*.db                     # Прочие базы
*.swp                    # Файлы временных сессий редакторов (например, Vim)

# 📦 Установки Poetry/PIP
poetry.lock              # Файл блокировок Poetry
dist/                    # Собранные дистрибутивы
build/                   # Сборочные файлы

# 🔍 Конфигурации IDE и редакторов
.vscode/                 # Настройки VSCode
.idea/                   # Настройки PyCharm/IntelliJ
*.iml                    # Файлы модулей IntelliJ

# 🐳 Docker
docker-compose.override.yml  # Локальная конфигурация Docker

# 💻 Операционные системы
.DS_Store                # macOS системный файл
Thumbs.db                # Windows системный файл

# 📓 Jupyter Notebooks
.ipynb_checkpoints/      # Чекпойнты Jupyter
